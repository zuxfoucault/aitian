# <Topic> — <one-line purpose>

<!-- A MAINTAINED doc: it must match the code. When a change touches a decision recorded here, update
     this file in the SAME change. Add this file to the "Docs" list in the root CLAUDE.md with a
     one-line description so future sessions can find it and load it only when relevant. -->

**Update when:** <!-- the concrete trigger that means this doc must be refreshed, e.g. "the build
pipeline or data schema changes." Pin the same trigger next to this doc in CLAUDE.md's Docs list. -->

## Decisions

- <!-- the decisions / rules this doc owns (the things the agent must honor) -->

## Notes

- <!-- detail the agent needs when working in this area -->
